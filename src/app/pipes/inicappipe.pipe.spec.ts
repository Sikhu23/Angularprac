import { InicappipePipe } from './inicappipe.pipe';

describe('InicappipePipe', () => {
  it('create an instance', () => {
    const pipe = new InicappipePipe();
    expect(pipe).toBeTruthy();
  });
});
